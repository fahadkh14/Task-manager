apiVersion: v1
kind: Service
metadata:
  name: frontend-task-service
  namespace: task
spec:
  type: NodePort
  selector:
    app: frontend-task
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
